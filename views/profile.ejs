<%- include('partials/header') %>
<%- include('partials/navbar') %>

<main class="profile-page-main">
  <div class="profile-container">
    
    <% if (typeof error !== 'undefined' && error) { %>
      <div class="alert alert-danger">
        <i class="fa-solid fa-triangle-exclamation"></i>
        <span><%= error %></span>
      </div>
    <% } %>

    <% if (typeof success !== 'undefined' && success) { %>
      <div class="alert alert-success">
        <i class="fa-solid fa-circle-check"></i>
        <span><%= success %></span>
      </div>
    <% } %>

    <div class="profile-layout-grid">
      
      <!-- Left Column: Quick view info card -->
      <section class="profile-sidebar">
        <div class="card profile-avatar-card">
          <div class="avatar-circle">
            <i class="fa-solid fa-user-tie"></i>
          </div>
          <h3><%= user.profile.fullName %></h3>
          <span class="profile-email-badge"><%= user.email %></span>
          <div class="sidebar-meta">
            <span class="track-tag"><i class="fa-solid fa-compass"></i> <%= user.profile.careerGoal %></span>
            <span class="year-tag"><%= user.profile.currentYear %></span>
          </div>
        </div>
      </section>

      <!-- Right Column: Edit Profile Form -->
      <section class="profile-form-section">
        <div class="card profile-form-card">
          <div class="card-header">
            <h3><i class="fa-solid fa-user-gear"></i> Account & Profile Settings</h3>
            <p>Update your details below to adjust your roadmap progress calculator</p>
          </div>
          <div class="card-body">
            <form action="/profile/update" method="POST" class="setup-form">
              <input type="hidden" name="_csrf" value="<%= csrfToken %>">
              <div class="form-grid">
                
                <!-- Full Name -->
                <div class="form-group">
                  <label for="fullName">Full Name <span class="required">*</span></label>
                  <div class="input-wrapper">
                    <i class="fa-solid fa-user"></i>
                    <input type="text" id="fullName" name="fullName" value="<%= user.profile.fullName %>" required>
                  </div>
                </div>

                <!-- College Name -->
                <div class="form-group">
                  <label for="collegeName">College Name <span class="required">*</span></label>
                  <div class="input-wrapper">
                    <i class="fa-solid fa-building-columns"></i>
                    <input type="text" id="collegeName" name="collegeName" value="<%= user.profile.collegeName %>" required>
                  </div>
                </div>

                <!-- Branch -->
                <div class="form-group">
                  <label for="branch">Branch/Major <span class="required">*</span></label>
                  <div class="input-wrapper">
                    <i class="fa-solid fa-graduation-cap"></i>
                    <input type="text" id="branch" name="branch" value="<%= user.profile.branch %>" required>
                  </div>
                </div>

                <!-- Current Year -->
                <div class="form-group">
                  <label for="currentYear">Current Year <span class="required">*</span></label>
                  <div class="input-wrapper">
                    <i class="fa-solid fa-calendar-days"></i>
                    <select id="currentYear" name="currentYear" required>
                      <option value="1st Year" <%= user.profile.currentYear === '1st Year' ? 'selected' : '' %>>1st Year</option>
                      <option value="2nd Year" <%= user.profile.currentYear === '2nd Year' ? 'selected' : '' %>>2nd Year</option>
                      <option value="3rd Year" <%= user.profile.currentYear === '3rd Year' ? 'selected' : '' %>>3rd Year</option>
                      <option value="4th Year" <%= user.profile.currentYear === '4th Year' ? 'selected' : '' %>>4th Year</option>
                    </select>
                  </div>
                </div>

                <!-- CGPA -->
                <div class="form-group">
                  <label for="cgpa">Current CGPA</label>
                  <div class="input-wrapper">
                    <i class="fa-solid fa-chart-bar"></i>
                    <input type="number" id="cgpa" name="cgpa" value="<%= user.profile.cgpa %>" step="0.01" min="0" max="10">
                  </div>
                </div>

                <!-- Career Goal -->
                <div class="form-group">
                  <label for="careerGoal">Target Career Goal <span class="required">*</span></label>
                  <div class="input-wrapper">
                    <i class="fa-solid fa-bullseye"></i>
                    <select id="careerGoal" name="careerGoal" required>
                      <option value="Software Engineer" <%= user.profile.careerGoal === 'Software Engineer' ? 'selected' : '' %>>Software Engineer</option>
                      <option value="Web Developer" <%= user.profile.careerGoal === 'Web Developer' ? 'selected' : '' %>>Web Developer</option>
                      <option value="AI Engineer" <%= user.profile.careerGoal === 'AI Engineer' ? 'selected' : '' %>>AI Engineer</option>
                      <option value="Cyber Security" <%= user.profile.careerGoal === 'Cyber Security' ? 'selected' : '' %>>Cyber Security</option>
                      <option value="Cloud Engineer" <%= user.profile.careerGoal === 'Cloud Engineer' ? 'selected' : '' %>>Cloud Engineer</option>
                      <option value="Data Scientist" <%= user.profile.careerGoal === 'Data Scientist' ? 'selected' : '' %>>Data Scientist</option>
                    </select>
                  </div>
                </div>

                <!-- Skills Selector -->
                <div class="form-group col-span-2">
                  <label for="skills">Current Skills <span class="label-hint">(Select multiple from the list below)</span></label>
                  
                  <!-- Hidden input to hold comma-separated values for backend compatibility -->
                  <input type="hidden" id="skills" name="skills" value="<%= user.profile.skills ? user.profile.skills.join(', ') : '' %>">
                  
                  <!-- Clickable Skill Tags Container -->
                  <div class="skills-selector-container">
                    <!-- Categories of Skills -->
                    <div class="skill-category">
                      <h5>Languages</h5>
                      <div class="skills-tags-list">
                        <% const languages = ['HTML', 'CSS', 'JavaScript', 'TypeScript', 'Python', 'Java', 'C++', 'Go', 'Rust', 'SQL']; %>
                        <% languages.forEach(skill => { %>
                          <span class="skill-tag-badge <%= (user.profile.skills && user.profile.skills.map(s => s.toLowerCase()).includes(skill.toLowerCase())) ? 'selected' : '' %>" data-skill="<%= skill %>">
                            <%= skill %>
                          </span>
                        <% }); %>
                      </div>
                    </div>

                    <div class="skill-category">
                      <h5>Web Development</h5>
                      <div class="skills-tags-list">
                        <% const web = ['React', 'Node.js', 'Express.js', 'MongoDB', 'Tailwind CSS', 'Bootstrap', 'REST APIs', 'GraphQL']; %>
                        <% web.forEach(skill => { %>
                          <span class="skill-tag-badge <%= (user.profile.skills && user.profile.skills.map(s => s.toLowerCase()).includes(skill.toLowerCase())) ? 'selected' : '' %>" data-skill="<%= skill %>">
                            <%= skill %>
                          </span>
                        <% }); %>
                      </div>
                    </div>

                    <div class="skill-category">
                      <h5>Data Science & AI</h5>
                      <div class="skills-tags-list">
                        <% const data = ['Pandas', 'NumPy', 'Machine Learning', 'Deep Learning', 'PyTorch', 'TensorFlow', 'Statistics']; %>
                        <% data.forEach(skill => { %>
                          <span class="skill-tag-badge <%= (user.profile.skills && user.profile.skills.map(s => s.toLowerCase()).includes(skill.toLowerCase())) ? 'selected' : '' %>" data-skill="<%= skill %>">
                            <%= skill %>
                          </span>
                        <% }); %>
                      </div>
                    </div>

                    <div class="skill-category">
                      <h5>DevOps & Cloud</h5>
                      <div class="skills-tags-list">
                        <% const devops = ['Git', 'GitHub', 'Docker', 'Kubernetes', 'AWS', 'Linux', 'Vercel', 'Netlify']; %>
                        <% devops.forEach(skill => { %>
                          <span class="skill-tag-badge <%= (user.profile.skills && user.profile.skills.map(s => s.toLowerCase()).includes(skill.toLowerCase())) ? 'selected' : '' %>" data-skill="<%= skill %>">
                            <%= skill %>
                          </span>
                        <% }); %>
                      </div>
                    </div>

                    <div class="skill-category">
                      <h5>Cybersecurity</h5>
                      <div class="skills-tags-list">
                        <% const cyber = ['Network Security', 'Cryptography', 'Pentesting', 'OWASP', 'Wireshark', 'Metasploit']; %>
                        <% cyber.forEach(skill => { %>
                          <span class="skill-tag-badge <%= (user.profile.skills && user.profile.skills.map(s => s.toLowerCase()).includes(skill.toLowerCase())) ? 'selected' : '' %>" data-skill="<%= skill %>">
                            <%= skill %>
                          </span>
                        <% }); %>
                      </div>
                    </div>

                    <% 
                    const allPredefined = [...languages, ...web, ...data, ...devops, ...cyber];
                    const customSkills = user.profile.skills ? user.profile.skills.filter(s => !allPredefined.map(p => p.toLowerCase()).includes(s.toLowerCase().trim())) : [];
                    %>
                    <% if (customSkills.length > 0) { %>
                      <div class="skill-category" id="custom-skills-group">
                        <h5>Custom Skills</h5>
                        <div class="skills-tags-list">
                          <% customSkills.forEach(skill => { %>
                            <span class="skill-tag-badge selected" data-skill="<%= skill %>">
                              <%= skill %>
                            </span>
                          <% }); %>
                        </div>
                      </div>
                    <% } %>
                  </div>

                  <!-- Custom Skill Input -->
                  <div class="custom-skill-adder">
                    <input type="text" id="custom-skill-input" placeholder="Type a custom skill...">
                    <button type="button" id="add-custom-skill-btn" class="btn btn-secondary btn-sm">Add Skill</button>
                  </div>
                  
                  <p class="form-hint-text">Add custom skills above, or click on predefined tags to toggle selection. They will automatically sync with your learning roadmap progress.</p>
                </div>

                <!-- Interests -->
                <div class="form-group col-span-2">
                  <label for="interests">Interests <span class="label-hint">(Comma-separated list)</span></label>
                  <div class="input-wrapper">
                    <i class="fa-solid fa-heart"></i>
                    <input type="text" id="interests" name="interests" value="<%= user.profile.interests ? user.profile.interests.join(', ') : '' %>">
                  </div>
                </div>

                <!-- Daily Study Hours -->
                <div class="form-group">
                  <label for="dailyStudyHours">Daily Study Hours</label>
                  <div class="input-wrapper">
                    <i class="fa-solid fa-clock"></i>
                    <input type="number" id="dailyStudyHours" name="dailyStudyHours" value="<%= user.profile.dailyStudyHours %>" min="0" max="24">
                  </div>
                </div>

              </div>

              <div class="form-footer-actions">
                <a href="/dashboard" class="btn btn-secondary">Cancel</a>
                <button type="submit" class="btn btn-primary">Save Changes <i class="fa-solid fa-floppy-disk"></i></button>
              </div>
            </form>
          </div>
        </div>
      </section>

    </div>
  </div>
</main>

<script>
  document.addEventListener('DOMContentLoaded', () => {
    const skillsInput = document.getElementById('skills');
    const customSkillInput = document.getElementById('custom-skill-input');
    const addCustomSkillBtn = document.getElementById('add-custom-skill-btn');
    const skillsSelectorContainer = document.querySelector('.skills-selector-container');

    // Parse existing skills
    let selectedSkills = skillsInput.value ? skillsInput.value.split(',').map(s => s.trim()).filter(s => s.length > 0) : [];

    // Helper to update the hidden input
    const updateSkillsInput = () => {
      skillsInput.value = selectedSkills.join(', ');
    };

    // Toggle badge click handler
    skillsSelectorContainer.addEventListener('click', (e) => {
      if (e.target.classList.contains('skill-tag-badge')) {
        const skill = e.target.getAttribute('data-skill');
        const skillIndex = selectedSkills.findIndex(s => s.toLowerCase() === skill.toLowerCase().trim());

        if (skillIndex > -1) {
          selectedSkills.splice(skillIndex, 1);
          e.target.classList.remove('selected');
        } else {
          selectedSkills.push(skill);
          e.target.classList.add('selected');
        }
        updateSkillsInput();
      }
    });

    // Add custom skill handler
    const addCustomSkill = () => {
      const customSkill = customSkillInput.value.trim();
      if (!customSkill) return;

      // Check if already exists (case-insensitive)
      const exists = selectedSkills.some(s => s.toLowerCase() === customSkill.toLowerCase());
      if (!exists) {
        selectedSkills.push(customSkill);
        updateSkillsInput();

        // Create a new badge in the Custom skills group
        let customGroup = document.getElementById('custom-skills-group');
        if (!customGroup) {
          customGroup = document.createElement('div');
          customGroup.className = 'skill-category';
          customGroup.id = 'custom-skills-group';
          customGroup.innerHTML = '<h5>Custom Skills</h5><div class="skills-tags-list"></div>';
          skillsSelectorContainer.appendChild(customGroup);
        }
        const tagsList = customGroup.querySelector('.skills-tags-list');
        const newBadge = document.createElement('span');
        newBadge.className = 'skill-tag-badge selected';
        newBadge.setAttribute('data-skill', customSkill);
        newBadge.innerText = customSkill;
        tagsList.appendChild(newBadge);
      }
      customSkillInput.value = '';
    };

    addCustomSkillBtn.addEventListener('click', addCustomSkill);
    customSkillInput.addEventListener('keypress', (e) => {
      if (e.key === 'Enter') {
        e.preventDefault();
        addCustomSkill();
      }
    });
  });
</script>

<%- include('partials/footer') %>
